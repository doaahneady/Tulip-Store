# 🔐 دليل الوصول إلى لوحة الموارد البشرية

## ✅ تم الإصلاح

تم إضافة رابط لوحة الموارد البشرية إلى قائمة المستخدم في الـ Navbar!

---

## 📍 كيفية الوصول

### الطريقة 1: من القائمة المنسدلة (الأسهل)
1. سجل الدخول إلى حسابك
2. اضغط على اسمك في الـ Navbar (أعلى الصفحة)
3. ستظهر قائمة منسدلة
4. ابحث عن **"لوحة الموارد البشرية"** مع أيقونة 👥
5. اضغط عليها

### الطريقة 2: الرابط المباشر
```
http://localhost:8000/hr/dashboard
```

### الطريقة 3: صفحة اختبار الصلاحيات
```
http://localhost:8000/test-permissions
```
هذه الصفحة تعرض:
- معلومات المستخدم
- جميع الصلاحيات
- الروابط المتاحة

---

## 🔑 التأكد من الصلاحيات

### 1. التحقق من قاعدة البيانات
```sql
SELECT id, name, email, is_hr, is_hr_manager FROM users WHERE id = 1;
```

يجب أن يكون:
- `is_hr = 1` (للوصول الأساسي)
- `is_hr_manager = 1` (للصلاحيات الكاملة)

### 2. إعطاء الصلاحيات

#### عبر SQL
```sql
UPDATE users SET is_hr = 1, is_hr_manager = 1 WHERE id = 1;
```

#### عبر Tinker
```bash
php artisan tinker
```
```php
$user = User::find(1);
$user->is_hr = true;
$user->is_hr_manager = true;
$user->save();
```

#### عبر phpMyAdmin
1. افتح phpMyAdmin
2. اختر قاعدة البيانات
3. افتح جدول `users`
4. عدّل المستخدم المطلوب
5. اجعل `is_hr` = 1
6. اجعل `is_hr_manager` = 1
7. احفظ

---

## 🎯 موقع الرابط في القائمة

القائمة المنسدلة تحتوي على:
```
┌─────────────────────────────────┐
│ الإعدادات                       │
│ الوضع الليلي                    │
│ اللغة                           │
│ طلباتي                          │
│ الإشعارات                       │
│ لوحة الإدارة (إذا is_admin)    │
│ لوحة IT (إذا is_it)            │
│ لوحة المحاسبة (إذا is_accountant)│
│ لوحة التوصيل (إذا is_driver_supervisor)│
│ 👥 لوحة الموارد البشرية ← هنا!  │
│ لوحة التحكم (إذا is_trader)    │
│ متجري (إذا is_trader)          │
│ تسجيل خروج                      │
└─────────────────────────────────┘
```

---

## 🐛 حل المشاكل

### المشكلة: الرابط لا يظهر في القائمة

**الحلول:**
1. تأكد من تسجيل الدخول
2. تأكد من `is_hr = 1` في قاعدة البيانات
3. امسح الكاش:
   ```bash
   php artisan cache:clear
   php artisan config:clear
   php artisan view:clear
   ```
4. أعد تحميل الصفحة بـ Ctrl+F5
5. جرب صفحة اختبار الصلاحيات: `/test-permissions`

### المشكلة: خطأ 404 عند الضغط على الرابط

**الحلول:**
1. تأكد من تشغيل الخادم: `php artisan serve`
2. تأكد من المسارات في `routes/web.php`
3. امسح الكاش: `php artisan route:clear`

### المشكلة: صفحة فارغة أو خطأ

**الحلول:**
1. تحقق من الأخطاء في `storage/logs/laravel.log`
2. تأكد من تشغيل Migration: `php artisan migrate`
3. تأكد من وجود البيانات التجريبية: `php artisan db:seed --class=HRSeeder`

---

## 📊 التحقق من النظام

### 1. اختبر الصلاحيات
```
http://localhost:8000/test-permissions
```

### 2. اختبر لوحة الموارد البشرية
```
http://localhost:8000/hr/dashboard
```

### 3. اختبر الصفحات الفرعية
```
http://localhost:8000/hr/employees
http://localhost:8000/hr/attendance
http://localhost:8000/hr/leaves
http://localhost:8000/hr/payroll
http://localhost:8000/hr/performance
http://localhost:8000/hr/training
http://localhost:8000/hr/reports
```

---

## ✅ قائمة التحقق

- [ ] تسجيل الدخول
- [ ] `is_hr = 1` في قاعدة البيانات
- [ ] مسح الكاش
- [ ] إعادة تحميل الصفحة
- [ ] الضغط على اسم المستخدم في Navbar
- [ ] رؤية "لوحة الموارد البشرية" في القائمة
- [ ] الضغط على الرابط
- [ ] الوصول إلى لوحة التحكم

---

## 🎉 النظام جاهز!

الآن يمكنك الوصول إلى لوحة الموارد البشرية من:
1. ✅ القائمة المنسدلة في Navbar
2. ✅ الرابط المباشر
3. ✅ صفحة اختبار الصلاحيات

---

## 📞 للمساعدة

إذا واجهت أي مشكلة:
1. تحقق من صفحة اختبار الصلاحيات: `/test-permissions`
2. راجع `storage/logs/laravel.log`
3. تأكد من تشغيل الخادم: `php artisan serve`
4. تأكد من قاعدة البيانات: `is_hr = 1`

**ملاحظة:** يجب أن يكون `is_hr = 1` في قاعدة البيانات لظهور الرابط!
